# 03 — Audio

Add **one** narration file to `02-audio/` (MP3, WAV, or M4A).

Recommended name: **`narration.mp3`**

No combine step — the pipeline uses that file directly for manifest timing and render.

Say **done** when the file is in place.
